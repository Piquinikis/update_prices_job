{
  "name": "update_prices_job",
  "version": "1.0.0",
  "main": "dist/job.js",
  "scripts": {
    "start": "npm run build && node dist/job.js",
    "start:dev": "nodemon",
    "test:unit": "cross-env TS_NODE_FILES=true mocha --exit --require ts-node/register --colors tests/**/*.ts",
    "test": "nyc npm run test:unit",
    "after-30-days": ""
  },
  "devDependencies": {
    "@types/chai": "^4.2.12",
    "@types/mocha": "^8.0.3",
    "@types/node": "^14.6.1",
    "chai": "^4.2.0",
    "cross-env": "^5.2.0",
    "mocha": "^8.1.2",
    "nyc": "^14.1.1",
    "nodemon": "^2.0.4",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.2"
  }
}
